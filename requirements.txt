# Dashboard deps ONLY — this file is what Streamlit Cloud installs.
# Keep it small and pinned: an unpinned dep picking up a breaking release
# during a cloud rebuild took the app down on 2026-07-10.
# Pipeline/training deps live in requirements-pipeline.txt (GitHub Actions).
streamlit==1.59.1
plotly>=5.23,<7
pandas>=2.2,<3
httpx>=0.27,<1
psycopg[binary]>=3.2,<4
python-dotenv>=1.0,<2
